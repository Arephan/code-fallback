{
  "name": "code-fallback",
  "version": "1.0.0",
  "description": "CLI wrapper that tries Claude for coding tasks, falls back to Codex on failure",
  "type": "module",
  "bin": {
    "code-fallback": "./bin/code-fallback.js"
  },
  "scripts": {
    "build": "tsc",
    "prepublishOnly": "npm run build"
  },
  "keywords": [
    "claude",
    "codex",
    "openai",
    "anthropic",
    "ai",
    "coding",
    "cli"
  ],
  "author": "Arephan",
  "license": "MIT",
  "dependencies": {
    "commander": "^12.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "typescript": "^5.3.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
